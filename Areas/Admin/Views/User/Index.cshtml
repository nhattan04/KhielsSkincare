@model IEnumerable<KhielsSkincare.Models.AppUser>
@{
    Layout = "_LayoutAdmin";
    ViewData["Title"] = "User";
}
<a asp-action="Create" asp-controller="Product" class="btn btn-primary">Thêm SP</a>

<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Danh sách user</h4>
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>Id</th>
                        <th>User Name</th>
                        <th>Email</th>
                        <th>Phone</th>
                        <th>PassWord Hash</th>
                        <th>Phone</th>
                        <th>Role</th>
                        <th>Chỉnh sửa</th>
                        <th>Xóa</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.Id</td>
                            <td>@item.UserName</td>
                            <td>@item.Email</td>
                            <td>@item.PhoneNumber</td>
                            <td>@item.PasswordHash</td>
                            <td></td>                           
                            <td>
                                <a asp-action="Edit" asp-controller="User" asp-route-Id="@item.Id" class="btn btn-warning">Edit</a>
                            </td>
                            <td>
                                <a asp-action="Delete" asp-controller="User" asp-route-id="@item.Id" class="btn btn-warning">Delete</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

<!-- Modal for delete confirmation -->
<div class="modal fade" id="deleteModal" tabindex="-1" aria-labelledby="deleteModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="deleteModalLabel">Xác nhận xóa</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                Bạn có chắc chắn muốn xóa sản phẩm <strong id="productName"></strong> không?
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Hủy</button>
                <form id="deleteForm" method="post" asp-action="DeleteConfirmed">
                    <input type="hidden" name="id" id="deleteProductId" />
                    <button type="submit" class="btn btn-danger">Xóa</button>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        function confirmDelete(productId, productName) {
            // Gán giá trị vào modal
            document.getElementById('deleteProductId').value = productId;
            document.getElementById('productName').textContent = productName;

            // Hiển thị modal
            var deleteModal = new bootstrap.Modal(document.getElementById('deleteModal'));
            deleteModal.show();
        }
    </script>
}
